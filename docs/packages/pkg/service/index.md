<!-- generated by go-indexer; do not edit manually -->

## Identity
- import_path: `github.com/livekit/livekit-server/pkg/service`
- source_dir: `pkg/service`
- package_name: `service`

## Summary
The service package owns LiveKit server service-layer wiring and runtime APIs, including HTTP/Twirp endpoints, PSRPC services, room lifecycle management, RTC signalling, auth, persistence adapters, ingress, egress, SIP, agent, TURN, and WHIP operations. It coordinates configuration, routing, RTC runtime objects, telemetry, stores, and protocol RPC clients into the server process.

## Responsibilities
- Construct and run the top-level LiveKit HTTP, Twirp, PSRPC, RTC, WHIP, agent, Prometheus, and TURN service surfaces.
- Authorize API calls, validate room and participant requests, and map service failures to HTTP, Twirp, or PSRPC errors.
- Manage rooms, participants, signalling sessions, room allocation, node selection, and local RTC room lifecycle.
- Persist and query rooms, participants, egress, ingress, SIP, and agent state through local or Redis-backed stores.
- Implement public service APIs for room, egress, ingress, SIP, agent dispatch, IO, RTC, WHIP, and signal relay workflows.

## Files
- file: `agent_dispatch_service.go`
  detail: `agent_dispatch_service.go.jsonl`
  summary: Implements the public agent dispatch service that validates room-admin access, creates rooms when needed, and forwards dispatch operations to the room-scoped internal agent dispatch client.
  responsibilities:
  - Create agent dispatch records with generated dispatch IDs.
  - Delete and list room-scoped agent dispatch records through the internal dispatch client.
  - Validate admin permissions and deployment names for dispatch operations.
  - Redact dispatch metadata before adding requests to structured logs.

- file: `agentservice.go`
  detail: `agentservice.go.jsonl`
  summary: Implements the service-side agent worker HTTP/WebSocket entry point, agent worker registry, internal agent RPC handlers, job assignment, worker draining, and worker signal dispatch integration.
  responsibilities:
  - Upgrade authenticated agent worker HTTP requests to WebSocket signalling connections.
  - Handshake and register agent workers with server and API key metadata.
  - Route agent job requests and termination requests to available workers.
  - Track worker availability by agent name, namespace, job type, and deployment.
  - Select workers by current load and deregister job and worker topics as workers leave.

- file: `auth.go`
  detail: `auth.go.jsonl`
  summary: Defines API key authentication middleware, authorization context helpers, bearer token constants, authentication errors, and room/SIP permission checks for service APIs.
  responsibilities:
  - Parse and verify bearer API tokens for HTTP service requests.
  - Store verified claim grants and API keys in request contexts.
  - Expose helpers for checking room, ingress, SIP, record, and destination-room permissions.
  - Convert authentication failures into HTTP or Twirp errors.

- file: `basic_auth.go`
  detail: `basic_auth.go.jsonl`
  summary: Defines an HTTP basic authentication middleware factory for protecting service endpoints with a configured username and password.
  responsibilities:
  - Create middleware closures that validate HTTP Basic Auth credentials.
  - Return 401 Unauthorized responses with a Basic realm challenge.
  - Delegate authenticated requests to the next HTTP handler.

- file: `clients.go`
  detail: `clients.go.jsonl`
  summary: Defines the IOClient service contract used to create, query, list, and update egress and ingress state through RPC-style calls.
  responsibilities:
  - Declare the IO client interface for egress and ingress service operations.
  - Specify request and response protobuf types used by IO service integrations.
  - Provide a counterfeiter target for generating IOClient test fakes.

- file: `egress.go`
  detail: `egress.go.jsonl`
  summary: Implements egress service operations for starting, updating, listing, and stopping egress jobs through egress launchers, IO service calls, room service calls, and typed egress RPC clients.
  responsibilities:
  - Start room, web, participant, track-composite, and track egress jobs.
  - Create and persist egress metadata after egress workers accept start requests.
  - Update egress layout and stream output settings.
  - List and stop egress jobs with permission checks and status-aware error handling.

- file: `egressid.go`
  detail: `egressid.go.jsonl`
  summary: Defines Twirp hook helpers for generating and storing an egress ID on start-egress requests before they reach the egress service.
  responsibilities:
  - Attach generated egress IDs to Twirp request contexts.
  - Expose helpers for storing and retrieving egress IDs from context.
  - Detect start-egress Twirp methods by routed method name.

- file: `errors.go`
  detail: `errors.go.jsonl`
  summary: Defines package-level PSRPC error sentinels used by service APIs for egress, ingress, room, participant, SIP, webhook, and connection validation failures.
  responsibilities:
  - Declare reusable service errors with PSRPC error codes.
  - Represent not-found, invalid-argument, internal, and failed-precondition service failures.
  - Share stable error values across room, ingress, egress, SIP, webhook, and signalling service paths.

- file: `ingress.go`
  detail: `ingress.go.jsonl`
  summary: Implements ingress administration for creating, launching, updating, listing, and deleting ingress endpoints through configured stores, IO RPCs, and ingress PSRPC clients.
  responsibilities:
  - Authorize ingress administration requests.
  - Create ingress records for RTMP, WHIP, and URL pull inputs.
  - Launch and update active ingress sessions through PSRPC clients.
  - Persist ingress metadata changes and emit ingress deletion telemetry.

- file: `interfaces.go`
  detail: `interfaces.go.jsonl`
  summary: Defines service-layer storage and allocation interfaces for rooms, participants, egress, ingress, SIP resources, and agent dispatch state.
  responsibilities:
  - Declare object-store contracts for room and participant persistence.
  - Declare service-store contracts for egress, ingress, SIP, and agent state persistence.
  - Declare room allocation operations used by room creation and node selection flows.

- file: `ioservice.go`
  detail: `ioservice.go.jsonl`
  summary: Implements the IO information service that exposes egress persistence operations over PSRPC and emits telemetry for egress lifecycle updates.
  responsibilities:
  - Create and manage the IOInfo PSRPC server.
  - Start Redis-backed egress cleanup when an egress store is configured.
  - Create, update, get, and list egress records through the egress store.
  - Emit egress lifecycle telemetry and accept placeholder SIP call updates.

- file: `ioservice_ingress.go`
  detail: `ioservice_ingress.go.jsonl`
  summary: Adds ingress persistence and state update handlers to IOInfoService, including ingress creation, lookup by ID or stream key, and telemetry updates for ingress state transitions.
  responsibilities:
  - Store newly created ingress records through the configured ingress store.
  - Load ingress information by ingress ID or stream key.
  - Persist ingress runtime state updates.
  - Emit ingress telemetry and logs when ingress state changes or is refreshed.

- file: `ioservice_sip.go`
  detail: `ioservice_sip.go.jsonl`
  summary: Adds SIP trunk matching, SIP dispatch rule evaluation, and inbound trunk authentication handlers to IOInfoService.
  responsibilities:
  - Match inbound SIP trunks for SIP calls by trunk ID or called number.
  - Select SIP trunks and dispatch rules through paged store iterators.
  - Evaluate SIP dispatch rules and build dispatch responses.
  - Resolve inbound trunk authentication prompts for SIP calls.

- file: `localstore.go`
  detail: `localstore.go.jsonl`
  summary: Implements an in-memory service store for rooms, room internals, participants, agent dispatches, and agent jobs.
  responsibilities:
  - Store and load room and room-internal state in local maps.
  - Store, list, and delete participant state by room and identity.
  - Provide local room locking with a process-wide mutex.
  - Store, list, and delete agent dispatch and job state for room-scoped agent operations.

- file: `redisstore.go`
  detail: `redisstore.go.jsonl`
  summary: Implements Redis-backed persistence for rooms, participants, egress, ingress, agent dispatches, and agent jobs, together with generic protobuf hash helpers.
  responsibilities:
  - Persist room, participant, egress, ingress, and agent state in Redis.
  - Coordinate room locks and ingress updates with Redis scripts and transactions.
  - Clean expired egress records in a background worker.
  - Provide generic protobuf hash loading, batching, sorting, and pagination helpers.

- file: `redisstore_sip.go`
  detail: `redisstore_sip.go.jsonl`
  summary: Implements Redis-backed persistence and compatibility handling for legacy, inbound, and outbound SIP trunks and SIP dispatch rules.
  responsibilities:
  - Store, load, list, and delete SIP trunk records in Redis.
  - Convert between legacy and directional SIP trunk representations.
  - Store, load, list, and delete SIP dispatch rules.
  - Apply request filtering, ID ordering, and pagination to SIP list operations.

- file: `roomallocator.go`
  detail: `roomallocator.go.jsonl`
  summary: Implements the standard room allocator that creates rooms, applies default and named room configuration, validates room creation, and assigns rooms to RTC nodes.
  responsibilities:
  - Create StandardRoomAllocator with router, node selector, configuration, and object store dependencies.
  - Create or update persisted room and internal room metadata under a room lock.
  - Select or preserve the RTC node assigned to a room.
  - Validate room creation and apply default or named room configuration values.

- file: `roommanager.go`
  detail: `roommanager.go.jsonl`
  summary: Implements the local RTC room manager that creates and deletes rooms, starts participant RTC sessions, hosts room and participant RPC services, manages participant state, and builds ICE/TURN configuration.
  responsibilities:
  - Create, hold, close, and delete local RTC rooms.
  - Start and resume participant RTC sessions with signalling, telemetry, ICE, and PSRPC participant services.
  - Implement room, participant, data, metadata, and agent dispatch RPC operations against local rooms.
  - Generate ICE server and refresh-token data for local participants.

- file: `roommanager_service.go`
  detail: `roommanager_service.go.jsonl`
  summary: Implements WHIP room-manager services for one-shot session creation, ingress connection notifications, media-state extraction, ICE trickle, ICE restart, and session deletion.
  responsibilities:
  - Create WHIP publisher sessions through RoomManager one-shot signalling.
  - Notify ingress about WHIP participant connection state and media metadata.
  - Extract basic input video and audio state from participant tracks.
  - Handle participant-scoped WHIP ICE trickle, ICE restart, and session deletion RPCs.

- file: `roomservice.go`
  detail: `roomservice.go.jsonl`
  summary: Implements the public room service API for creating, listing, deleting, and mutating rooms and participants through routing, stores, and typed RPC clients.
  responsibilities:
  - Authorize and record room service requests and responses.
  - Create, list, delete, and update rooms through room allocator, router, store, and room RPC client dependencies.
  - Validate request limits for room names, participant names, metadata, attributes, and data nonces.
  - Forward participant-scoped operations to typed participant RPC clients.

- file: `rtcservice.go`
  detail: `rtcservice.go.jsonl`
  summary: Implements the RTC HTTP service for WebSocket signalling routes, join-request validation, participant connection startup, signal message proxying, and connection draining.
  responsibilities:
  - Register RTC WebSocket and validation routes.
  - Validate legacy and v1 RTC join requests into routing participant initialization data.
  - Upgrade validated HTTP requests to WebSocket signalling connections.
  - Proxy signal requests and responses between WebSocket clients and routing message pipes.
  - Track and drain active WebSocket connections.

- file: `server.go`
  detail: `server.go.jsonl`
  summary: Implements the top-level LiveKit HTTP server that wires Twirp APIs, RTC and WHIP routes, agent routes, Prometheus metrics, node registration, startup, shutdown, debug endpoints, and health checks.
  responsibilities:
  - Construct the LiveKit HTTP and Prometheus servers with middleware and service routes.
  - Start router, IO, signal, HTTP, Prometheus, TURN, and background room cleanup services.
  - Drain and stop the server while coordinating room, signal, IO, and router shutdown.
  - Expose node, room manager, debug, default, and health-check handlers.

- file: `signal.go`
  detail: `signal.go.jsonl`
  summary: Defines the PSRPC signal relay server, session handler contract, default session routing handler, and relay stream adapters used to bridge remote signalling streams into local room sessions.
  responsibilities:
  - Create and manage typed PSRPC signal relay servers.
  - Route participant start-session requests to the correct RTC node.
  - Bridge relay signal streams into routing message channels and message sinks.
  - Adapt relay signal request and response protobuf messages for routing helpers.

- file: `sip.go`
  detail: `sip.go.jsonl`
  summary: Implements the SIP management service for SIP trunks, dispatch rules, outbound SIP participant creation, and SIP participant transfer requests.
  responsibilities:
  - Authorize SIP admin and SIP call operations.
  - Create, update, list, load, and delete SIP trunk records through SIPStore.
  - Create, update, list, and delete SIP dispatch rules with SIP validation helpers.
  - Build outbound SIP participant and transfer requests and send them to the SIP RPC client.

- file: `turn.go`
  detail: `turn.go.jsonl`
  summary: Builds and configures the embedded TURN server and implements TURN username and password authentication for LiveKit participants.
  responsibilities:
  - Create TURN listeners and packet connections from server configuration.
  - Configure relay address generators, telemetry wrappers, and peer permission checks.
  - Generate expiring participant-scoped TURN usernames and passwords.
  - Authenticate TURN requests against API key secrets and credential expiry rules.

- file: `twirp.go`
  detail: `twirp.go.jsonl`
  summary: Defines Twirp server hooks for API request logging, Prometheus request status metrics, and telemetry APICall reporting.
  responsibilities:
  - Build Twirp server hooks for logging, status metrics, and telemetry.
  - Attach per-request state to contexts with private context key structs.
  - Record service, method, status, error, timeout, request, and response metadata for API observability.

- file: `utils.go`
  detail: `utils.go.jsonl`
  summary: Provides HTTP, gzip, client metadata, room configuration, connection validation, user-agent parsing, and service route utility functions.
  responsibilities:
  - Decompress bounded gzip payloads and write HTTP error responses.
  - Parse and augment client information from request parameters, headers, and user-agent data.
  - Apply room configuration and validate participant connection requests.
  - Identify RTC and agent HTTP endpoint paths.

- file: `whipservice.go`
  detail: `whipservice.go.jsonl`
  summary: Implements the WHIP HTTP service for participant session creation, ICE trickle and restart PATCH handling, session deletion, and WHIP-specific request validation.
  responsibilities:
  - Register WHIP HTTP routes for participant sessions.
  - Validate WHIP create requests, grants, room and participant limits, client info headers, and SDP offers.
  - Proxy WHIP create and participant session operations to room routing and typed RPC clients.
  - Translate WHIP participant RPC errors into HTTP responses and log WHIP API outcomes.

- file: `wire.go`
  detail: `wire.go.jsonl`
  summary: Defines Wire injector functions and provider helpers for constructing LiveKit server, router, stores, message bus, service configuration slices, SIP client, TURN server, and runtime support dependencies.
  responsibilities:
  - Declare the Wire provider graph for the full LiveKit server.
  - Declare the Wire provider graph for router-only construction.
  - Select local or Redis-backed storage and message bus implementations.
  - Expose configuration and dependency provider helpers used by Wire.

- file: `wsprotocol.go`
  detail: `wsprotocol.go.jsonl`
  summary: Implements WebSocket-backed signalling and worker protocol I/O for LiveKit service connections, including protobuf/JSON negotiation, periodic pings, close handling, and expected close-error detection.
  responsibilities:
  - Wrap WebSocket clients as signalling connections.
  - Read and write SignalRequest, SignalResponse, WorkerMessage, and ServerMessage payloads.
  - Switch between protobuf and JSON encoding based on incoming WebSocket message type.
  - Send periodic ping control frames and classify expected WebSocket closure errors.

## Dependency Package
- dependency_package: `github.com/livekit/livekit-server/pkg/agent`
  package_name: `agent`
  index_path: `docs/packages/pkg/agent/index.md`
  relation:
  - Constructs agent clients and tracks agent worker protocol behavior.
  - Integrates agent workers, job routing, and agent dispatch state with service APIs.

- dependency_package: `github.com/livekit/livekit-server/pkg/clientconfiguration`
  package_name: `clientconfiguration`
  index_path: `docs/packages/pkg/clientconfiguration/index.md`
  relation:
  - Provides client configuration selection used when creating RTC participants.
  - Supplies static client configuration defaults for room manager sessions.

- dependency_package: `github.com/livekit/livekit-server/pkg/config`
  package_name: `config`
  index_path: `docs/packages/pkg/config/index.md`
  relation:
  - Supplies server, room, RTC, API, limit, ingress, SIP, TURN, signal relay, Prometheus, and node stats configuration.
  - Drives route setup, permission limits, storage choices, media behavior, TURN setup, and Wire providers.

- dependency_package: `github.com/livekit/livekit-server/pkg/routing`
  package_name: `routing`
  index_path: `docs/packages/pkg/routing/index.md`
  relation:
  - Provides routers, message routers, node identity, signal clients, room manager clients, and participant signalling pipes.
  - Used to allocate rooms, start sessions, relay signals, and select local or remote RTC nodes.

- dependency_package: `github.com/livekit/livekit-server/pkg/routing/selector`
  package_name: `selector`
  index_path: `docs/packages/pkg/routing/selector/index.md`
  relation:
  - Provides node selection strategies for room allocation.
  - Used by service utilities and StandardRoomAllocator when choosing RTC nodes.

- dependency_package: `github.com/livekit/livekit-server/pkg/rtc`
  package_name: `rtc`
  index_path: `docs/packages/pkg/rtc/index.md`
  relation:
  - Provides room, participant, WebRTC configuration, egress launcher, and RTC signalling runtime primitives.
  - Used by room manager, room service, WHIP service, RTC service, egress service, and utility validation paths.

- dependency_package: `github.com/livekit/livekit-server/pkg/rtc/types`
  package_name: `types`
  index_path: `docs/packages/pkg/rtc/types/index.md`
  relation:
  - Provides participant interfaces, protocol version helpers, close reasons, media resolver results, and data message cache types.
  - Used by room manager, RTC service, WHIP room-manager service, WebSocket protocol handling, and utility helpers.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu`
  package_name: `sfu`
  index_path: `docs/packages/pkg/sfu/index.md`
  relation:
  - Provides forward statistics collection used by room manager participant sessions.
  - Supplies Wire provider output for optional RTC forwarding statistics.

- dependency_package: `github.com/livekit/livekit-server/pkg/telemetry`
  package_name: `telemetry`
  index_path: `docs/packages/pkg/telemetry/index.md`
  relation:
  - Provides telemetry services for API, room, participant, egress, ingress, SIP, TURN, and IO lifecycle events.
  - Used when constructing service runtime dependencies and emitting service-level analytics.

- dependency_package: `github.com/livekit/livekit-server/pkg/telemetry/prometheus`
  package_name: `prometheus`
  index_path: `docs/packages/pkg/telemetry/prometheus/index.md`
  relation:
  - Provides Prometheus counters and observers for rooms, participants, RTC initialization, ingress, TURN, and API status reporting.
  - Used by room manager, room-manager WHIP service, signal relay, Twirp hooks, RTC service, TURN service, and server metrics paths.

- dependency_package: `github.com/livekit/livekit-server/pkg/utils`
  package_name: `utils`
  index_path: `docs/packages/pkg/utils/index.md`
  relation:
  - Provides request logging, ICE config caching, endpoint utilities, client metadata helpers, and context helpers used by service code.
  - Supports RTC connection handling, room manager ICE cache, WHIP logging, Twirp logging, and signal service utilities.
