<!-- generated by go-indexer; do not edit manually -->

## Identity
- import_path: `github.com/livekit/livekit-server/pkg/config`
- source_dir: `pkg/config`
- package_name: `config`

## Summary
Defines LiveKit server configuration schema, defaults, YAML and CLI loading, API key validation, generated CLI flags, and logger initialization helpers.

## Responsibilities
- Model server, RTC, room, TURN, API, logging, limits, metrics, tracing, node stats, and integration configuration.
- Provide default values used as the base configuration for server startup.
- Merge YAML configuration and CLI overrides into a validated Config.
- Generate hidden or visible CLI flags from YAML-tagged config fields.
- Validate API key configuration and initialize the global logger.

## Files
- file: `config.go`
  detail: `config.go.jsonl`
  summary: Defines LiveKit server configuration structs, defaults, YAML and CLI loading, API key validation, generated CLI flags, and logger initialization helpers.
  responsibilities:
  - Model server, RTC, room, TURN, API, logging, limits, metrics, tracing, and node stats configuration.
  - Provide default configuration values for server startup.
  - Merge YAML config and CLI overrides into a validated Config.
  - Generate CLI flags from YAML-tagged config fields.
  - Validate API key configuration and initialize logging from config.

## Dependency Package
- dependency_package: `github.com/livekit/livekit-server/pkg/agent`
  package_name: `agent`
  index_path: `docs/packages/pkg/agent/index.md`
  relation:
  - Embeds agent worker configuration in the top-level server config.
  - Uses agent default target load in DefaultConfig.
- dependency_package: `github.com/livekit/livekit-server/pkg/metric`
  package_name: `metric`
  index_path: `docs/packages/pkg/metric/index.md`
  relation:
  - Embeds metric subsystem configuration in Config.
  - Uses metric default configuration in DefaultConfig.
- dependency_package: `github.com/livekit/livekit-server/pkg/sfu`
  package_name: `sfu`
  index_path: `docs/packages/pkg/sfu/index.md`
  relation:
  - Embeds SFU audio, PLI throttle, stream tracker, and forwarding stats configuration.
  - Uses SFU defaults for audio, PLI throttle, and stream tracker settings.
- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/bwe/remotebwe`
  package_name: `remotebwe`
  index_path: `docs/packages/pkg/sfu/bwe/remotebwe/index.md`
  relation:
  - Embeds receive-side bandwidth estimator configuration.
  - Uses the default remote BWE configuration in RTC congestion control defaults.
- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/bwe/sendsidebwe`
  package_name: `sendsidebwe`
  index_path: `docs/packages/pkg/sfu/bwe/sendsidebwe/index.md`
  relation:
  - Embeds send-side bandwidth estimator configuration.
  - Uses the default send-side BWE configuration in RTC congestion control defaults.
- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/pacer`
  package_name: `pacer`
  index_path: `docs/packages/pkg/sfu/pacer/index.md`
  relation:
  - Uses pacer behavior constants to default send-side BWE pacer mode.
  - Connects RTC congestion control configuration to SFU pacing behavior.
- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/streamallocator`
  package_name: `streamallocator`
  index_path: `docs/packages/pkg/sfu/streamallocator/index.md`
  relation:
  - Embeds stream allocator configuration in RTC congestion control settings.
  - Uses the stream allocator default configuration in DefaultConfig.
