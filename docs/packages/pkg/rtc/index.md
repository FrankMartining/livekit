<!-- generated by go-indexer; do not edit manually -->

## Identity
- import_path: `github.com/livekit/livekit-server/pkg/rtc`
- source_dir: `pkg/rtc`
- package_name: `rtc`

## Summary
The rtc package owns LiveKit room and local participant runtime behavior, including signaling, WebRTC transport coordination, media and data track publication, subscription management, migration, and room broadcasts. It connects protocol messages and participant state to SFU media forwarding, telemetry, routing, egress, and agent dispatch integrations.

## Responsibilities
- Create and manage rooms, local participants, participant state, permissions, metadata, and lifecycle transitions.
- Coordinate publisher and subscriber transports, SDP negotiation, ICE handling, reconnect, fallback, and migration behavior.
- Publish media and data tracks, resolve subscriptions, dispatch RTP/RTCP and data messages, and maintain track metadata.
- Broadcast participant, room, speaker, data, subscription, and signaling updates to clients and room listeners.
- Enforce runtime limits and feature gates while reporting telemetry, metrics, connection quality, and egress failures.

## Files
- file: `clientinfo.go`
  detail: `clientinfo.go.jsonl`
  summary: Wraps LiveKit client metadata and exposes SDK, browser, OS, version, and capability predicates used by RTC behavior gates.
  responsibilities:
  - Identify client browser, SDK, and operating system categories.
  - Expose capability predicates for codec, transport, reconnect, transceiver, packet trailer, and data channel behavior.
  - Compare semantic SDK versions for feature gates.

- file: `config.go`
  detail: `config.go.jsonl`
  summary: Defines RTC WebRTC configuration wrappers, media direction configuration structures, RTP header extension and RTCP feedback defaults, and constructors for publisher and subscriber transport settings.
  responsibilities:
  - Wrap the shared rtcconfig WebRTC configuration with LiveKit RTC receiver, publisher, subscriber, and buffer factory settings.
  - Define RTP header extension and RTCP feedback configuration structures for audio and video directions.
  - Build default publisher and subscriber media configuration based on congestion control and consolidated transport settings.

- file: `datadowntrack.go`
  detail: `datadowntrack.go.jsonl`
  summary: Implements DataDownTrack, which forwards published data track packets to one subscriber through a data track transport.
  responsibilities:
  - Create subscriber-specific data down tracks and register them with the published data track.
  - Expose data down track identity, handle, and source track accessors.
  - Rewrite packet handles, marshal data track packets, and send them through the transport.
  - Remove the down track from the published data track on close.

- file: `datatrack.go`
  detail: `datatrack.go.jsonl`
  summary: Implements published data tracks for RTC participants, including subscriber data downtrack creation, downtrack fanout, lifecycle closure, and data track statistics updates.
  responsibilities:
  - Expose published data track identity and protobuf metadata.
  - Create and remove subscriber-specific data downtracks.
  - Register and unregister data track senders for packet fanout.
  - Broadcast incoming data track packets to subscribed downtracks.
  - Track data packet statistics and close lifecycle state.

- file: `datatrack_stats.go`
  detail: `datatrack_stats.go.jsonl`
  summary: Tracks packet, frame, byte, loss, out-of-order, and duration statistics for RTC data tracks and logs a final summary when closed.
  responsibilities:
  - Maintain data track packet and byte counters.
  - Detect duplicate, out-of-order, and missing packet sequence numbers.
  - Log data track duration, frame rate, loss, ordering, and byte totals on close.

- file: `egress.go`
  detail: `egress.go.jsonl`
  summary: Builds and starts automatic participant and track egress requests, emits failed egress telemetry events, and normalizes direct file output paths for track egress.
  responsibilities:
  - Define the egress launcher contract used by RTC auto-egress helpers.
  - Start participant and track egress through the protocol RPC request types.
  - Notify telemetry when automatic egress startup fails.
  - Build direct file output paths that include track IDs when needed.

- file: `errors.go`
  detail: `errors.go.jsonl`
  summary: Defines package-level sentinel errors used by room, participant, permission, transport, data channel, track subscription, and metrics subscription flows.
  responsibilities:
  - Declare room and participant lifecycle errors.
  - Declare permission, capacity, and configuration limit errors.
  - Declare data channel and transport errors.
  - Declare track subscription and metrics subscription errors.

- file: `mediaengine.go`
  detail: `mediaengine.go.jsonl`
  summary: Builds and filters WebRTC media engine codec and RTP header extension configuration for RTC publisher and subscriber transports.
  responsibilities:
  - Register enabled audio and video codecs with WebRTC media engines.
  - Register configured RTP header extensions for audio and video.
  - Select fallback audio and video codecs from enabled codec lists.
  - Filter codec parameter lists by enabled codec configuration and RTCP feedback settings.
  - Detect H.264 high profile FMTP values for optional filtering.

- file: `medialossproxy.go`
  detail: `medialossproxy.go.jsonl`
  summary: Aggregates downstream media loss reports from local downtracks and subscriber nodes, throttles updates, and emits the highest observed fractional loss through a callback.
  responsibilities:
  - Track the maximum downstream fractional loss over a reporting window.
  - Accept RTCP receiver report loss feedback from downtracks.
  - Accept subscriber node media loss notifications.
  - Throttle media loss update callbacks to a fixed interval.

- file: `mediatrack.go`
  detail: `mediatrack.go.jsonl`
  summary: Implements local published media tracks, tying WebRTC RTP receivers to SFU receivers, dynacast subscription control, media loss handling, codec regression, telemetry, and track lifecycle operations.
  responsibilities:
  - Construct media track receivers with audio, video, dynacast, and codec regression behavior.
  - Add WebRTC RTP receivers and bind their buffers, RTCP handling, codec state, and telemetry callbacks.
  - Expose track metadata, CID lookup, mute, restart, close, and connection quality operations.
  - Forward dynacast subscription changes to signaling callbacks and receiver spatial-layer limits.

- file: `mediatrackreceiver.go`
  detail: `mediatrackreceiver.go.jsonl`
  summary: Implements the media track receiver core for published RTC media tracks, including receiver state, codec receiver setup, codec regression, subscriber creation, track info mutation, simulcast layer metadata, quality selection, and RTP statistics aggregation.
  responsibilities:
  - Maintain active and potential SFU track receivers for a published media track.
  - Create wrapped receivers for subscribers and manage subscribed track lifecycles.
  - Update track metadata for mute state, codecs, layers, SSRCs, RIDs, SDP CIDs, and video sizes.
  - Handle codec regression and receiver cleanup across close and resume flows.
  - Expose track identity, receiver lookup, quality selection, audio level, debug information, and aggregated RTP stats.

- file: `mediatracksubscriptions.go`
  detail: `mediatracksubscriptions.go.jsonl`
  summary: Manages subscriber state for a published media track, including subscribed track creation, transceiver reuse, downtrack binding, subscriber removal, mute propagation, video-layer refresh, and debug export.
  responsibilities:
  - Create subscribed tracks for local participants and attach them to wrapped receivers.
  - Reuse cached transceivers when possible or add new local tracks when needed.
  - Track active subscribers by participant ID.
  - Close subscribed downtracks when subscribers are removed.
  - Propagate publisher mute and video-layer updates to subscribed tracks.

- file: `migrationdatacache.go`
  detail: `migrationdatacache.go.jsonl`
  summary: Implements a data packet cache used during participant migration to hold reliable packets until the expected next sequence number arrives or the wait expires.
  responsibilities:
  - Represent migration data cache states for waiting, timeout, and done outcomes.
  - Cache data packets that arrive after a sequence gap during migration.
  - Detect when the expected next sequence arrives and unblock processing.
  - Expose cached packets for later processing after migration wait state resolves.

- file: `participant.go`
  detail: `participant.go.jsonl`
  summary: Defines ParticipantImpl, the core local participant implementation for signalling, permissions, media publishing, subscriptions, transport management, migration, data channels, metrics, and RPC.
  responsibilities:
  - Create and manage local participant lifecycle, state, permissions, metadata, migration, and reconnection.
  - Coordinate publisher and subscriber transports with media tracks, data tracks, subscriptions, RTCP, and connection quality.
  - Handle signalling messages, data packets, metrics, codec negotiation, reliable data replay, and data-channel RPC.

- file: `participant_data_track.go`
  detail: `participant_data_track.go.jsonl`
  summary: Handles participant data track signaling requests and received data track packets, including publish validation, unpublish handling, subscription updates, packet dispatch, and subscribed data track handle allocation.
  responsibilities:
  - Validate and publish participant data tracks from signaling requests.
  - Unpublish participant data tracks by publication handle.
  - Forward data subscription updates to participant listeners.
  - Parse received data track packets and dispatch them to published data tracks and listeners.
  - Allocate non-zero handles for subscribed data tracks.

- file: `participant_sdp.go`
  detail: `participant_sdp.go.jsonl`
  summary: Provides ParticipantImpl helpers for matching publisher SDP media sections to track codec ids, setting publisher codec preferences, and adjusting publisher SDP answers for audio features.
  responsibilities:
  - Populate SDP codec ids for pending and already published tracks.
  - Configure publisher transceiver receiver codec preferences from unmatched SDP media sections.
  - Rewrite publisher answers with audio DTX and stereo fmtp settings.

- file: `participant_signal.go`
  detail: `participant_signal.go.jsonl`
  summary: Implements ParticipantImpl signalling output helpers for join, participant updates, speaker updates, reconnect, ICE, SDP, track events, leave requests, subscription responses, media section requirements, and data track responses.
  responsibilities:
  - Write participant signalling messages through the signaller response sink.
  - Maintain participant update cache and queueing around join readiness.
  - Filter signalling messages by client protocol capabilities.
  - Build reconnect, leave, ICE, SDP, subscription, and data track signalling responses.
  - Close or swap signalling sinks for participant connections.

- file: `room.go`
  detail: `room.go.jsonl`
  summary: Defines the rtc Room aggregate, participant lifecycle management, room-level event listeners, agent dispatch integration, participant update batching, and room data broadcast helpers.
  responsibilities:
  - Manage local participants, joins, resumes, removals, room closure, and room metadata updates.
  - Coordinate published media and data track subscriptions, room broadcasts, telemetry callbacks, and worker loops.
  - Launch and track room, participant, and publisher agent jobs while exposing helper functions for participant updates and data forwarding.

- file: `roomtrackmanager.go`
  detail: `roomtrackmanager.go.jsonl`
  summary: Owns room-level media and data track registration, lookup, observer notification, removal notification, and track count reporting.
  responsibilities:
  - Store media and data tracks by LiveKit track ID with publisher identity metadata.
  - Deduplicate repeated track registrations for the same concrete track instance.
  - Resolve the active track info for a track ID and ignore closing media tracks.
  - Notify observers when tracks change or are removed.
  - Report media and data track map sizes.

- file: `signalanddatastats.go`
  detail: `signalanddatastats.go.jsonl`
  summary: Tracks byte and message statistics for data and signaling paths, periodically reports telemetry, and creates synthetic signal track reporting state after room and participant resolution.
  responsibilities:
  - Represent synthetic byte track types for data and signaling telemetry.
  - Accumulate send and receive byte and message totals with atomics.
  - Periodically flush data track statistics to participant telemetry listeners.
  - Manage deferred signal telemetry reporting for room and participant lifecycle events.
  - Generate deterministic synthetic track IDs for participant byte tracks.

- file: `subscribedtrack.go`
  detail: `subscribedtrack.go.jsonl`
  summary: Implements subscriber-side media track state for one subscribed track, including downtrack construction, bind callbacks, subscriber settings, negotiation flags, telemetry, and DownTrack listener events.
  responsibilities:
  - Create SFU downtracks for subscribed media tracks.
  - Track bind, close, negotiation, RTP sender, and subscriber setting state.
  - Apply subscriber mute, quality, spatial layer, temporal layer, and adaptive stream settings.
  - Report downstream stats and stream-start telemetry.
  - Forward downtrack listener events to participant and media track callbacks.

- file: `subscriptionmanager.go`
  detail: `subscriptionmanager.go.jsonl`
  summary: Manages media and data track subscriptions for a local participant, including desired subscription state, reconciliation workers, source track resolution, permission handling, subscriber lifecycle, telemetry, and subscription cleanup.
  responsibilities:
  - Track desired and active media/data subscriptions by track ID.
  - Reconcile asynchronous subscribe and unsubscribe state against track resolver results.
  - Create and remove media subscribed tracks and data downtracks for local participants.
  - Track publisher subscription status, subscription limits, notifier observers, and cancellation metrics.
  - Record subscription telemetry and notify participants about data track subscriber handles.

- file: `transport.go`
  detail: `transport.go.jsonl`
  summary: Defines PCTransport, the peer connection wrapper used by rtc to manage WebRTC negotiation, ICE, data channels, RTP sender setup, and transport lifecycle state.
  responsibilities:
  - Create and configure Pion peer connections for publish and subscribe transports.
  - Coordinate SDP offer-answer negotiation, ICE trickle, ICE restart, and migration recovery.
  - Manage data channels, stream allocator integration, codec preferences, and transport health callbacks.

- file: `transportmanager.go`
  detail: `transportmanager.go.jsonl`
  summary: Defines TransportManager, which coordinates publisher and subscriber PCTransport instances, ICE fallback and reconfiguration, data channel setup, reconnect handling, migration state, and transport-level RTT and media-loss signals.
  responsibilities:
  - Create and route operations between publisher, subscriber, single-PC, and one-shot signalling transports.
  - Manage ICE configuration, TCP or TURN fallback, reconnect handling, and transport failure escalation.
  - Forward tracks, RTCP, data messages, data channels, SDP offers and answers, and migration state to the appropriate PCTransport.

- file: `updatatrackmanager.go`
  detail: `updatatrackmanager.go.jsonl`
  summary: Manages published upstream data tracks for a participant, including registration, removal, lookup, packet dispatch, and protobuf export.
  responsibilities:
  - Store published data tracks by publication handle.
  - Notify participant listeners when data tracks are published or unpublished.
  - Close and clear data tracks when the manager is closed.
  - Dispatch received data track packets to the matching published data track.
  - Export published data track state as LiveKit protobuf track info.

- file: `uptrackmanager.go`
  detail: `uptrackmanager.go.jsonl`
  summary: Manages a participant's published upstream media tracks, subscription permissions, and related callbacks for RTC participant state.
  responsibilities:
  - Store and expose published media tracks for a participant.
  - Maintain versioned subscription permission state and per-subscriber track access.
  - Notify track update and manager close callbacks.
  - Revoke subscriptions when permissions change.
  - Export track state and debug information.

- file: `user_packet_deduper.go`
  detail: `user_packet_deduper.go.jsonl`
  summary: Implements a bounded nonce-based deduper for user packets using UUID nonces and a linked map window.
  responsibilities:
  - Track recently seen user packet nonces.
  - Detect duplicate packets by UUID nonce.
  - Maintain a bounded recent nonce window.
  - Ignore packets whose nonce cannot be parsed as a UUID.

- file: `utils.go`
  detail: `utils.go.jsonl`
  summary: Provides RTC utility helpers for packing stream identifiers, mapping track source and kind values, panic recovery, contextual logger enrichment, IP truncation, protobuf batch chunking, and RED codec support detection.
  responsibilities:
  - Pack and unpack participant, stream, track, and data track labels.
  - Map protocol and WebRTC track values into RTC grouping and protobuf values.
  - Recover panics with logger reporting.
  - Attach participant, room, track, transport, and codec metadata to loggers.
  - Truncate public IP addresses for logging and split protobuf batches by encoded size.

- file: `wrappedreceiver.go`
  detail: `wrappedreceiver.go.jsonl`
  summary: Provides receiver wrappers that defer SFU track receiver selection until codec negotiation and preserve downtracks, callbacks, and settings while a real receiver is not yet available.
  responsibilities:
  - Wrap simulcast receivers behind a negotiated TrackReceiver selection.
  - Expose dummy receivers that can queue downtracks and callbacks before upgrade.
  - Provide RED and primary audio receiver adapters for Opus and RED codec translation.

## Dependency Package
- dependency_package: `github.com/livekit/livekit-server/pkg/agent`
  package_name: `agent`
  index_path: `docs/packages/pkg/agent/index.md`
  relation:
  - Dispatches room, participant, and publisher jobs from room lifecycle events.
  - Tracks agent dispatch state for room-owned agent integrations.

- dependency_package: `github.com/livekit/livekit-server/pkg/config`
  package_name: `config`
  index_path: `docs/packages/pkg/config/index.md`
  relation:
  - Supplies RTC, room, egress, and participant runtime configuration.
  - Provides codec, transport, telemetry, and capacity settings consumed by room and participant setup.

- dependency_package: `github.com/livekit/livekit-server/pkg/metric`
  package_name: `metric`
  index_path: `docs/packages/pkg/metric/index.md`
  relation:
  - Records RTC participant, room, track, transport, and data-channel metrics.
  - Supplies metric labels and collectors used by stats and lifecycle paths.

- dependency_package: `github.com/livekit/livekit-server/pkg/routing`
  package_name: `routing`
  index_path: `docs/packages/pkg/routing/index.md`
  relation:
  - Resolves room, participant, and node routing information for RTC operations.
  - Supplies routing messages used by room and subscription coordination.

- dependency_package: `github.com/livekit/livekit-server/pkg/rtc/datatrack`
  package_name: `datatrack`
  index_path: `docs/packages/pkg/rtc/datatrack/index.md`
  relation:
  - Provides data channel sender and transport contracts for RTC data track fanout.
  - Supplies packet framing helpers used by published and subscribed data tracks.

- dependency_package: `github.com/livekit/livekit-server/pkg/rtc/dynacast`
  package_name: `dynacast`
  index_path: `docs/packages/pkg/rtc/dynacast/index.md`
  relation:
  - Controls dynacast video layer subscription behavior for published media tracks.
  - Connects subscriber interest changes to publisher layer availability decisions.

- dependency_package: `github.com/livekit/livekit-server/pkg/rtc/signalling`
  package_name: `signalling`
  index_path: `docs/packages/pkg/rtc/signalling/index.md`
  relation:
  - Provides signalling response sinks, async queues, and message delivery helpers.
  - Carries participant join, offer-answer, ICE, track, and subscription responses.

- dependency_package: `github.com/livekit/livekit-server/pkg/rtc/supervisor`
  package_name: `supervisor`
  index_path: `docs/packages/pkg/rtc/supervisor/index.md`
  relation:
  - Supervises participant and transport operations that need retry or failure handling.
  - Coordinates long-running RTC worker lifecycles.

- dependency_package: `github.com/livekit/livekit-server/pkg/rtc/transport`
  package_name: `transport`
  index_path: `docs/packages/pkg/rtc/transport/index.md`
  relation:
  - Defines transport callback contracts implemented by RTC participant and transport manager code.
  - Supplies negotiation state values used by WebRTC transport coordination.

- dependency_package: `github.com/livekit/livekit-server/pkg/rtc/types`
  package_name: `types`
  index_path: `docs/packages/pkg/rtc/types/index.md`
  relation:
  - Provides shared RTC room, participant, track, transport, and callback interfaces.
  - Supplies typed metadata and listener contracts exchanged across RTC components.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu`
  package_name: `sfu`
  index_path: `docs/packages/pkg/sfu/index.md`
  relation:
  - Provides upstream receivers, downtracks, RTCP handling, RTP forwarding, and media stats.
  - Connects published RTC tracks to subscriber media delivery.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/buffer`
  package_name: `buffer`
  index_path: `docs/packages/pkg/sfu/buffer/index.md`
  relation:
  - Supplies RTP buffer factories, packet metadata, video layer values, and stream stats.
  - Provides buffering data used by media receivers, tracks, and transport setup.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/bwe`
  package_name: `bwe`
  index_path: `docs/packages/pkg/sfu/bwe/index.md`
  relation:
  - Supplies bandwidth estimation configuration values used by transport and media setup.
  - Connects RTC WebRTC configuration to SFU bandwidth estimation mode selection.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/bwe/remotebwe`
  package_name: `remotebwe`
  index_path: `docs/packages/pkg/sfu/bwe/remotebwe/index.md`
  relation:
  - Provides REMB-based remote bandwidth estimation integration for subscriber transports.
  - Supplies estimator factories used by transport setup.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/bwe/sendsidebwe`
  package_name: `sendsidebwe`
  index_path: `docs/packages/pkg/sfu/bwe/sendsidebwe/index.md`
  relation:
  - Provides send-side bandwidth estimation integration for subscriber transports.
  - Supplies transport feedback handling used by stream allocation and pacing.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/connectionquality`
  package_name: `connectionquality`
  index_path: `docs/packages/pkg/sfu/connectionquality/index.md`
  relation:
  - Computes connection quality from media, data, and transport statistics.
  - Supplies quality scores and updates reported by participants and tracks.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/datachannel`
  package_name: `datachannel`
  index_path: `docs/packages/pkg/sfu/datachannel/index.md`
  relation:
  - Provides data channel abstractions used by participant RPC and RTC data transport.
  - Supplies data channel state and handlers for reliable and lossy messages.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/interceptor`
  package_name: `interceptor`
  index_path: `docs/packages/pkg/sfu/interceptor/index.md`
  relation:
  - Supplies WebRTC interceptor setup for media transport behavior.
  - Provides RTP/RTCP hooks used by RTC transport configuration.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/pacer`
  package_name: `pacer`
  index_path: `docs/packages/pkg/sfu/pacer/index.md`
  relation:
  - Provides packet pacing used by subscriber transports and media downtracks.
  - Connects transport sender setup to paced RTP delivery.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/rtpextension/abscapturetime`
  package_name: `abscapturetime`
  index_path: `docs/packages/pkg/sfu/rtpextension/abscapturetime/index.md`
  relation:
  - Handles absolute capture time RTP extension data used by media forwarding.
  - Supports capture-time metadata in track receiver and downtrack flows.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/rtpextension/dependencydescriptor`
  package_name: `dependencydescriptor`
  index_path: `docs/packages/pkg/sfu/rtpextension/dependencydescriptor/index.md`
  relation:
  - Parses and carries dependency descriptor RTP extension data for video tracks.
  - Supports codec negotiation, layer tracking, and receiver metadata updates.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/rtpextension/playoutdelay`
  package_name: `playoutdelay`
  index_path: `docs/packages/pkg/sfu/rtpextension/playoutdelay/index.md`
  relation:
  - Provides playout delay RTP extension parsing and serialization.
  - Supports subscriber media delay configuration in RTC and SFU paths.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/rtpstats`
  package_name: `rtpstats`
  index_path: `docs/packages/pkg/sfu/rtpstats/index.md`
  relation:
  - Supplies RTP statistics structures used by media tracks, receivers, and telemetry.
  - Provides snapshot and delta stats consumed by participant reporting.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/streamallocator`
  package_name: `streamallocator`
  index_path: `docs/packages/pkg/sfu/streamallocator/index.md`
  relation:
  - Allocates subscriber video layers and bandwidth across active downtracks.
  - Provides stream state updates routed through RTC transport and participant callbacks.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/utils`
  package_name: `utils`
  index_path: `docs/packages/pkg/sfu/utils/index.md`
  relation:
  - Supplies SFU media utility helpers used by RTC track and transport code.
  - Provides codec, RTP, and layer helper behavior shared with RTC integrations.

- dependency_package: `github.com/livekit/livekit-server/pkg/telemetry`
  package_name: `telemetry`
  index_path: `docs/packages/pkg/telemetry/index.md`
  relation:
  - Reports room, participant, track, egress, subscription, and data statistics events.
  - Supplies telemetry listener contracts used throughout RTC lifecycle handling.

- dependency_package: `github.com/livekit/livekit-server/pkg/telemetry/prometheus`
  package_name: `prometheus`
  index_path: `docs/packages/pkg/telemetry/prometheus/index.md`
  relation:
  - Provides Prometheus metrics registered from RTC media, transport, and room paths.
  - Supplies collectors used for runtime RTC observability.

- dependency_package: `github.com/livekit/livekit-server/pkg/utils`
  package_name: `utils`
  index_path: `docs/packages/pkg/utils/index.md`
  relation:
  - Provides shared utility helpers for concurrency, timers, logging, and protocol operations.
  - Supports RTC lifecycle code with reusable repository-level helper behavior.
