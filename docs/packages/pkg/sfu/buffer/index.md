<!-- generated by go-indexer; do not edit manually -->

## Identity
- import_path: `github.com/livekit/livekit-server/pkg/sfu/buffer`
- source_dir: `pkg/sfu/buffer`
- package_name: `buffer`

## Summary
This package owns RTP and RTCP buffering for SFU media streams. It manages packet storage, RTP stats, NACK state, codec and layer metadata, dependency descriptor parsing, frame-rate calculation, video size tracking, RTX repair mapping, and buffer lifecycle callbacks.

## Responsibilities
- Construct RTP buffers, RTCP readers, and SSRC-indexed buffer factories.
- Bind RTP parameters, codecs, payload types, header extensions, stats, and packet buckets.
- Process incoming RTP packets into extended packets with sequence adjustment, NACK state, bucket storage, video metadata, and read notifications.
- Generate or expose RTCP-related state such as NACK pairs, reception reports, sender report data, and PLI requests.
- Track video layers, frame integrity, frame rates, video sizes, audio levels, data byte stats, and stream restarts.

## Files
- file: `buffer.go`
  detail: `buffer.go.jsonl`
  summary: Owns the publisher-side RTP buffer, pre-bind packet queue, RTX repair path, RTCP feedback generation, and buffer lifecycle callbacks.
  responsibilities:
  - Construct RTP buffers backed by BufferBase.
  - Queue packets until codec and RTP parameters are bound.
  - Write media and RTX packets into buffer accounting.
  - Generate RTCP feedback including NACK, receiver reports, and PLI.
  - Expose read, close, and callback hooks for upstream SFU components.

- file: `buffer_base.go`
  detail: `buffer_base.go.jsonl`
  summary: Owns the shared RTP buffer core used by publisher and subscriber-facing buffers, including RTP stats, NACK state, packet bucket storage, extended packet creation, codec and layer metadata, callbacks, stream restart handling, and key-frame seeding.
  responsibilities:
  - Configure codec, RTP parameters, packet bucket, stats, NACK, and header extension state.
  - Accept RTP packets and update stats, NACK, sequence adjustments, bucket storage, and extended packet queues.
  - Parse video metadata including dependency descriptors, video layers, key frames, video sizes, and frame rates.
  - Expose RTP stats, delta stats, sender report data, NACK pairs, audio levels, and packet retrieval.
  - Manage close, restart, callbacks, read notification, and key-frame seeding lifecycle.

- file: `datastats.go`
  detail: `datastats.go.jsonl`
  summary: Owns byte-count statistics for data streams, including active-window and aggregate RTPStats protobuf conversion.
  responsibilities:
  - Track total bytes and active-window bytes.
  - Reset active-window counters after the configured duration.
  - Mark aggregate stats end time when stopped.
  - Convert active and aggregate byte counters to livekit RTPStats.

- file: `dependencydescriptorparser.go`
  detail: `dependencydescriptorparser.go.jsonl`
  summary: Owns dependency descriptor RTP extension parsing, stateful frame and decode-target tracking, restart handling, and helper utilities for decode target masks and attached video sizes.
  responsibilities:
  - Pool parsed dependency descriptor wrapper objects.
  - Parse dependency descriptor RTP header extensions into buffer-level metadata.
  - Track extended RTP sequence numbers and frame numbers across wraparound.
  - Update frame dependency structures and active decode targets.
  - Expose helpers for decode target masks, video sizes, and descriptor lifecycle release.

- file: `factory.go`
  detail: `factory.go.jsonl`
  summary: Owns construction and lookup of RTP buffers and RTCP readers by SSRC, including delayed RTX repair-to-base buffer pairing.
  responsibilities:
  - Store packet tracking configuration used by new buffer factories.
  - Create RTP buffers and RTCP readers on demand.
  - Return existing buffer objects for repeated SSRC lookups.
  - Maintain RTX repair-to-base SSRC mappings.
  - Remove buffer and reader entries when they close.

- file: `fps.go`
  detail: `fps.go.jsonl`
  summary: Owns frame-rate calculators for VP8, VP9, dependency-descriptor streams, and H26x streams using RTP packet metadata and frame dependency state.
  responsibilities:
  - Define shared frame-rate calculation thresholds and frame tracking state.
  - Expose the FrameRateCalculator interface used by buffer code.
  - Calculate VPx frame rates from PictureID progression.
  - Calculate dependency-descriptor frame rates by spatial and temporal layer.
  - Calculate H26x frame rates from continuous timestamp and sequence-number runs.

- file: `frameintegrity.go`
  detail: `frameintegrity.go.jsonl`
  summary: Owns packet history and frame integrity tracking for dependency-descriptor frames.
  responsibilities:
  - Track first and last packet sequence numbers for frames.
  - Record packet receipt history in a circular bitset.
  - Check whether packets in a frame range are consecutive.
  - Track integrity state for a bounded window of frame numbers.

- file: `rtcpreader.go`
  detail: `rtcpreader.go.jsonl`
  summary: Owns a lightweight RTCP reader/writer adapter that invokes packet and close callbacks.
  responsibilities:
  - Store RTCP reader SSRC and close state.
  - Invoke packet callbacks when RTCP bytes are written.
  - Invoke close callback exactly once.
  - Satisfy read/write closer style interfaces used by buffer factories.

- file: `streamstats.go`
  detail: `streamstats.go.jsonl`
  summary: Owns the stream statistics container that groups aggregate, per-layer, and remote-view RTP delta statistics.
  responsibilities:
  - Represent aggregate RTP delta stats for a stream.
  - Represent per-spatial-layer RTP delta stats.
  - Carry receiver-report remote-view RTP delta stats.

- file: `videolayer.go`
  detail: `videolayer.go.jsonl`
  summary: Owns video layer identifiers, default layer values, and layer comparison helpers.
  responsibilities:
  - Define invalid and default spatial and temporal layer constants.
  - Expose invalid and maximum VideoLayer values.
  - Represent video spatial and temporal layer pairs.
  - Compare and validate video layer values.

- file: `videolayerutils.go`
  detail: `videolayerutils.go.jsonl`
  summary: Owns helpers for mapping video RIDs, spatial layers, video qualities, and TrackInfo layer metadata for simulcast and SVC streams.
  responsibilities:
  - Define known RID spaces used for video layers.
  - Derive layer quality presence from TrackInfo metadata.
  - Convert between RIDs, spatial layers, and video qualities.
  - Select codec-specific video layers and layer mode by MIME type.
  - Normalize RID ordering into known layer spaces.

## Dependency Package
- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/audio`
  package_name: `audio`
  index_path: `docs/packages/pkg/sfu/audio/index.md`
  relation:
  - Provides audio level configuration and audio level tracking used by BufferBase.
  - Supplies audio level state updated from RTP audio level header extensions.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/rtpextension/abscapturetime`
  package_name: `abscapturetime`
  index_path: `docs/packages/pkg/sfu/rtpextension/abscapturetime/index.md`
  relation:
  - Provides parsing for the absolute capture time RTP header extension.
  - Supplies metadata attached to ExtPacket values when the extension is present.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/rtpextension/dependencydescriptor`
  package_name: `dependencydescriptor`
  index_path: `docs/packages/pkg/sfu/rtpextension/dependencydescriptor/index.md`
  relation:
  - Provides dependency descriptor RTP extension parsing types and errors.
  - Supplies frame dependency structures, decode target metadata, and descriptor constants used for layer and integrity tracking.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/rtpstats`
  package_name: `rtpstats`
  index_path: `docs/packages/pkg/sfu/rtpstats/index.md`
  relation:
  - Provides RTP receiver stats, flow state, delta stats, lite stats, and reception report generation.
  - Supplies stats data used for NACK updates, sender reports, bucket growth, and exported RTPStats.

- dependency_package: `github.com/livekit/livekit-server/pkg/sfu/utils`
  package_name: `utils`
  index_path: `docs/packages/pkg/sfu/utils/index.md`
  relation:
  - Provides RangeMap utilities used to track sequence number adjustments after dropped padding-only packets.
  - Supports packet bucket sequence mapping in BufferBase.

- dependency_package: `github.com/livekit/livekit-server/pkg/utils`
  package_name: `utils`
  index_path: `docs/packages/pkg/utils/index.md`
  relation:
  - Provides logger component constants used when constructing publisher-side RTP buffers.
  - Supplies shared utility identifiers for SFU and publisher logging context.
